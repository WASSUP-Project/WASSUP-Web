export type member = {
    id: number;
    name: string;
    phoneNumber: string;
    birth: string;
    specifics: string;
};

export type memberForAttendance = {
    id: number;
    name: string;
    status: number;
};